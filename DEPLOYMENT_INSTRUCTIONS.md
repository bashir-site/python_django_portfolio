# Инструкция по развертыванию python_django_portfolio

## ✅ Текущая ситуация

Ваш проект уже настроен для работы с существующим nginx контейнером из `pdf-translator`:
- ✅ Конфигурация `portfolio.conf` уже подключена в `nginx.conf` (строка 262)
- ✅ Volume для портфолио уже настроен в `docker-compose.prod.yml`
- ✅ Nginx контейнер уже запущен и работает

## 📋 Шаги для развертывания

### 1. Скопировать файлы портфолио на сервер

На сервере выполните:

```bash
# Перейти в домашнюю директорию
cd /home/bashir

# Создать директорию для портфолио (если не существует)
sudo mkdir -p /var/www/portfolio

# Скопировать все файлы портфолио
sudo cp -r python_django_portfolio/* /var/www/portfolio/

# Установить правильные права доступа
sudo chown -R www-data:www-data /var/www/portfolio
sudo chmod -R 755 /var/www/portfolio
```

### 2. Проверить конфигурацию nginx

Убедитесь, что в `pdf-translator/docker-compose.prod.yml` есть:

```yaml
nginx:
  volumes:
    - ./nginx/portfolio.conf:/etc/nginx/conf.d/portfolio.conf:ro
    - /var/www/portfolio:/var/www/portfolio:ro
```

### 3. Перезагрузить nginx контейнер

```bash
cd /home/bashir/pdf-translator
docker compose restart nginx

# Или если используете docker-compose (старая версия):
# docker-compose restart nginx
```

### 4. Проверить SSL сертификаты

Если домен `bashir-python-backend-django-fastapi.ru` еще не настроен:

```bash
# Получить SSL сертификат через certbot
sudo certbot certonly --webroot \
  -w /var/www/certbot \
  -d bashir-python-backend-django-fastapi.ru \
  -d www.bashir-python-backend-django-fastapi.ru
```

### 5. Проверить работу

```bash
# Проверить логи nginx
docker logs translate_files_nginx

# Проверить доступность портфолио
curl -I https://bashir-python-backend-django-fastapi.ru
```

## 🔄 Обновление портфолио в будущем

Для обновления файлов портфолио:

```bash
# На сервере
cd /home/bashir
sudo cp -r python_django_portfolio/* /var/www/portfolio/
sudo chown -R www-data:www-data /var/www/portfolio
docker compose -f pdf-translator/docker-compose.prod.yml restart nginx
```

## 📝 Альтернативный вариант: монтирование напрямую

Если хотите монтировать портфолио напрямую из домашней директории (без копирования):

### Обновить docker-compose.prod.yml:

```yaml
nginx:
  volumes:
    - ./nginx/portfolio.conf:/etc/nginx/conf.d/portfolio.conf:ro
    - /home/bashir/python_django_portfolio:/var/www/portfolio:ro  # Прямое монтирование
```

Затем перезапустить:
```bash
cd /home/bashir/pdf-translator
docker compose up -d nginx
```

## ✅ Является ли это хорошей практикой?

**ДА, это отличная практика!** Вот почему:

### Преимущества использования одного nginx:

1. **Экономия ресурсов**: Один nginx контейнер вместо двух
2. **Единая точка управления**: Все конфигурации в одном месте
3. **Общие SSL сертификаты**: Можно использовать один certbot
4. **Простота обслуживания**: Один контейнер для мониторинга
5. **Эффективное использование портов**: Один nginx на портах 80/443

### Когда использовать отдельные nginx:

- Разные команды разработки для каждого проекта
- Разные требования к безопасности/изоляции
- Очень разные конфигурации (например, один нужен proxy, другой только статика)
- Масштабирование проектов независимо

### Рекомендация для вашего случая:

**Использовать один nginx** - это правильный выбор, потому что:
- Оба проекта на одном сервере
- Один разработчик (вы)
- Портфолио - статический сайт, не требует отдельного nginx
- Экономия ресурсов на маленьком VPS важна

## 🚀 Оптимизация для быстрой загрузки

Ваш портфолио уже оптимизирован:
- ✅ Все шрифты локальные (нет запросов к Google Fonts)
- ✅ Все ресурсы локальные (нет внешних CDN)
- ✅ Service Worker для кеширования
- ✅ Оптимизированная конфигурация nginx с кешированием

## 🔍 Проверка работы

После развертывания проверьте:

1. **Доступность сайта:**
   ```bash
   curl -I https://bashir-python-backend-django-fastapi.ru
   ```

2. **Проверка статических файлов:**
   ```bash
   curl -I https://bashir-python-backend-django-fastapi.ru/assets/css/style.css
   ```

3. **Проверка шрифтов:**
   ```bash
   curl -I https://bashir-python-backend-django-fastapi.ru/assets/fonts/open-sans/Regular.woff2
   ```

4. **Проверка в браузере:**
   - Откройте DevTools → Network
   - Убедитесь, что нет запросов к внешним ресурсам
   - Все файлы должны загружаться с вашего домена

## 📊 Мониторинг

Для мониторинга логов портфолио:

```bash
# Логи доступа портфолио
docker exec translate_files_nginx tail -f /var/log/nginx/portfolio_access.log

# Логи ошибок портфолио
docker exec translate_files_nginx tail -f /var/log/nginx/portfolio_error.log
```

## 🛠️ Troubleshooting

### Проблема: 502 Bad Gateway
```bash
# Проверить права доступа
sudo ls -la /var/www/portfolio
sudo chown -R www-data:www-data /var/www/portfolio
```

### Проблема: 404 Not Found
```bash
# Проверить, что файлы скопированы
sudo ls -la /var/www/portfolio/index.html

# Проверить конфигурацию nginx
docker exec translate_files_nginx nginx -t
```

### Проблема: SSL ошибки
```bash
# Проверить сертификаты
sudo ls -la /etc/letsencrypt/live/bashir-python-backend-django-fastapi.ru/

# Перезапустить nginx
docker compose -f pdf-translator/docker-compose.prod.yml restart nginx
```
